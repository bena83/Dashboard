const ix = document.getElementById("icon")
ix.addEventListener("click", () => {
    document.getElementById("clicked")
    ix.classList.toggle = "cancel"
})

function myFunction(x) {
    x.classList.toggle("change");
}